# Session Learnings — 2026-01-02

## Output Format for Obsidian

- **Code blocks preserve raw markdown** — Wrapping output in ` ```markdown ``` ` lets users copy raw syntax that pastes correctly into Obsidian (headings, checkboxes intact)
- **Terminal rendering strips markdown** — Without code blocks, the CLI renders markdown visually, so copy-paste loses `##`, `- [ ]` syntax
- **Leading space quirk** — Code block copy adds leading space to each line; Shift+Tab in Obsidian fixes this
- **No blank lines between sections** — CSS heading styles provide vertical spacing; blank lines are unnecessary

## Daily Notes Integration

- **Dataview inline fields** — Format like `- \`Via   \`:: [[link]]` for queryable metadata
- **First-person narrative** — When user is a speaker in thread, summarize with "I" not wiki-link to self
- **Omit self from Who field** — User's Daily Note doesn't need their own name in participants

## Task Formatting

- **Be aggressive inferring tasks** — Better to over-identify implied tasks (user deletes unwanted) than miss important ones
- **Always include Tasks section** — Even if empty (write "None"), keeps format consistent
- **Task markers**: 🔺 for priority (commitments/decisions), `[[expect]]` prefix for watching others

## Reference Data Architecture

- **Separate reference file** — `slack-reference.md` keeps lookup tables (channels, people, glossary) separate from skill logic
- **User-editable mappings** — User can update channels, people, glossary without touching skill prompts
- **Wiki-link aliases** — Format `[[Full Name|Alias]]` displays alias in Obsidian

## Slack-Specific

- **Channel detection** — Message input placeholder (e.g., "Message 🔒translation-system") reveals channel name
- **Date detection** — Date picker in screenshot header provides date
- **Ask if not visible** — Channel and date may not be in screenshot; prompt user before processing
- **DM vs Channel format** — DMs use person name in header (no project emoji), channels use project

## Workflow

- **Session files should be local** — `/par-*` commands use relative paths; files go to current working directory's `docs/sessions/`
- **Launch Claude Code from project folder** — Ensures project-specific commands available and session docs local
