# Session Decisions — 2026-01-02

## Decision 1: JSON Extraction Schema (Internal Only)

**Context**: Deciding whether to add structured intermediate format for thread processing

**Options Considered**:
1. Continue outputting markdown directly
2. Add explicit JSON schema, output both JSON and markdown
3. Add JSON schema for internal use only, output markdown ← Chosen

**Decision**: Document JSON schema in PURPOSE.md; skill constructs it mentally before formatting. JSON is not output to user.

**Rationale**: Structured extraction ensures consistency without cluttering output. Schema documented for maintainability.

---

## Decision 2: Daily Notes Output Format

**Context**: Integrating skill output with user's existing Obsidian Daily Notes format

**Options Considered**:
1. Keep original generic format (## Summary, ## Decisions, etc.)
2. Match user's manual format exactly ← Chosen

**Decision**: Output matches Daily Notes structure with ### header, Dataview metadata block, #### Discussion, #### Tasks

**Rationale**: Seamless integration with existing workflow; no reformatting needed after paste

---

## Decision 3: Separate Reference File for Lookups

**Context**: Where to store channel/people/glossary mappings

**Options Considered**:
1. Embed tables in r-slack.md skill
2. Create separate slack-reference.md file ← Chosen

**Decision**: Lookups live in `.claude/commands/slack-reference.md`

**Rationale**: User can edit mappings without touching skill logic; cleaner separation of concerns

---

## Decision 4: Task Identification Approach

**Context**: How aggressively to identify tasks from threads

**Options Considered**:
1. Conservative — only explicit commitments/requests
2. Aggressive — infer implied tasks ← Chosen

**Decision**: Be aggressive about inferring tasks; always include Tasks section even if empty

**Rationale**: Better to over-identify (user deletes unwanted) than miss important tasks

---

## Decision 5: First-Person Narrative

**Context**: How to refer to user (Fraser) in output

**Options Considered**:
1. Use wiki-link [[Fraser Gorrie]] consistently
2. Use "I" when summarizing Fraser's actions ← Chosen

**Decision**: First-person "I" for Fraser's statements/actions; omit self from Who field

**Rationale**: More natural reading in personal Daily Notes

---

## Decision 6: DM Header Format

**Context**: How to format header for DM threads (no channel/project)

**Options Considered**:
1. Use project based on workspace
2. Use person name, no project emoji ← Chosen

**Decision**: DM headers: `### 💬 Slack • Gabriel DM • 10:17` (no project/emoji)

**Rationale**: DMs don't belong to projects; person name is the identifying context

---

## Decision 7: Compact Output (No Blank Lines)

**Context**: Vertical spacing between sections in output

**Options Considered**:
1. Blank lines between sections for readability
2. No blank lines — CSS handles spacing ← Chosen

**Decision**: Remove all blank lines between sections

**Rationale**: User's Obsidian CSS provides heading spacing; blank lines add unnecessary height
