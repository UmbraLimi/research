# Conv Learnings - 2026-03-21

## 1. Backtick Pre-computation Verified Working in r-start and r-end
**Topics:** skills, cc-workflow

**Context:** User reported that both /r-start and /r-end had been failing due to a backtick problem. Investigated the skills and their referenced scripts.

**Learning:** All `!` backtick commands in r-start and r-end SKILL.md files expanded correctly in this conv. The scripts in `.claude/scripts/` (conv-read-counter.sh, conv-read-current.sh) exist, are executable, and return correct values. The machine-name file at `~/.claude/.machine-name` is also present and correct. Whatever was causing the previous failures appears to be resolved — possibly by the Conv 005 bulk-sync that aligned skills from peerloop-docs improvements.

---

## 2. Comm-Helpers r-slack Output Format Fix
**Topics:** workflow

**Context:** Uncommitted changes in Comm-Helpers were found at conv start and committed before proceeding.

**Learning:** The r-slack skill's file output was changed to write from scratch (cat > file) instead of editing an existing file, and code fences were removed from example output so markdown pastes directly into Obsidian without needing manual cleanup.
